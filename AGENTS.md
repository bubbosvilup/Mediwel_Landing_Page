## Test frontend

- Non usare il browser integrato di Codex.
- Usa Playwright con Google Chrome (`channel: "chrome"`) e `headless: false`.
- Avvia il server locale e verifica ogni modifica nel Chrome visibile.
- Se Playwright o Chrome non sono disponibili, segnalalo senza ripiegare sul browser integrato.